1. Array Rotation - Write a function rotate(ar[], d, n) that rotates arr[] of size n by d elements.
2. Write a function rotate(arr[], d, n) that rotates arr[] of size n by d elements using reversal.
3. Given an array, cyclically rotate the array clockwise by one.
4. Search an element in a sorted and rotated array
5. Find if there is a pair with a given sum in array
6. Given a sorted and rotated array, find if there is a pair with a given sum
7. Find maximum value of Sum( i*arr[i]) with only rotations on given array allowed
